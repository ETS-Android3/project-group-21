name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'LibrarySystem-Frontend/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # you can add testing for your application here with npm test
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "secrets.c436e3c1-f996-4601-b4d4-320486cb52e4"
          heroku_app_name: "library-backend-21gp" #Must be unique in Heroku
          heroku_email: "liyujinjin@hotmail.com"
          dontuseforce: false
